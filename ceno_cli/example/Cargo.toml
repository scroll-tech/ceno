[workspace]
[package]
name = "ceno_example"
edition = "2021"
license = "MIT OR Apache-2.0"
version = "0.1.0"

[dependencies]
ceno_rt = { path = "../../ceno_rt" }
rkyv = { version = "0.8", default-features = false, features = [
    "alloc",
    "bytecheck",
] }
